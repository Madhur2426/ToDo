export interface Task {
    id:number;//The unique identifier for the task.
    task:string;// The description or name of the task.
    completed:boolean;// A boolean flag indicating whether the task is completed or not.
}
